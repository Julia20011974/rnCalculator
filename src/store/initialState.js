import {darkTheme} from 'theme';

const initialState = {
    calculator: {
        displayValue: '0',
        displayAnswer: '',
    },
    history: [],
    theme: darkTheme,
};
export default initialState;
